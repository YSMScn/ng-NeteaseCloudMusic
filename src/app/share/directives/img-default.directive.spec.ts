import { ImgDefaultDirective } from './img-default.directive';

describe('ImgDefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgDefaultDirective();
    expect(directive).toBeTruthy();
  });
});
